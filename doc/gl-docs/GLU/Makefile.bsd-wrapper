# $OpenBSD: Makefile.bsd-wrapper,v 1.2 2006/11/30 17:24:57 matthieu Exp $

all:	includes
	$(MAKE)

depend:
	lndir ${.CURDIR}
	if [ -x $(X11BASE)/bin/xmkmf ]; then \
		$(X11BASE)/bin/xmkmf ;\
	fi

install:
	$(MAKE) install.man

.include <bsd.xorg.mk>
